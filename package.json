{
  "name": "react-router-with-refine",
  "version": "0.0.0",
  "scripts": {
    "dev": "npm run dev -w app",
    "build": "npm run build -w app",
    "start": "npm run start -w app",
    "deploy": "npm run deploy -w infra",
    "destroy": "npm run destroy -w infra",
    "diff": "npm run diff -w infra"
  },
  "workspaces": [
    "app",
    "infra"
  ],
  "devDependencies": {
    "prettier": "^3.4.2"
  }
}
